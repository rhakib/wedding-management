
const ServiceOption = ({ item }) => {

    console.log(item);
    return (
        <div>

            <ul>
                <li>{item}</li>
            </ul>

        </div>
    );
};

export default ServiceOption;